/**
 * AI Response Generator для Telegram-бота рекрутера
 *
 * АРХИТЕКТУРА:
 * Система работает в 3 этапа с четкой логикой перехода между ними:
 *
 * 1. AWAITING_PIN (Ожидание PIN-кода)
 *    - Кандидат не идентифицирован
 *    - AI запрашивает 4-значный код из письма
 *    - Переход: когда кандидат отправляет PIN → система проверяет → создает conversation → переход к PIN_RECEIVED
 *
 * 2. PIN_RECEIVED (PIN получен - начало интервью)
 *    - Кандидат только что идентифицирован по PIN
 *    - AI приветствует и сразу просит голосовое сообщение
 *    - Использует данные из резюме для персонализации вопросов
 *    - Переход: после первого ответа → INTERVIEWING
 *
 * 3. INTERVIEWING (Проведение интервью)
 *    - Полноценное интервью с идентифицированным кандидатом
 *    - AI задает вопросы, собирает информацию, предлагает следующие шаги
 *    - Если нет голосовых - приоритетно просит записать
 *    - Если есть голосовые - задает уточняющие вопросы
 *
 * ИСПОЛЬЗОВАНИЕ:
 * - text-message.ts: проверяет наличие conversation → AWAITING_PIN или INTERVIEWING
 * - unidentified-message.ts: обрабатывает PIN → создает conversation → PIN_RECEIVED
 */

import { generateText } from "@qbs-autonaim/lib";

type ConversationStage =
  | "AWAITING_PIN" // Ждем PIN-код от кандидата
  | "PIN_RECEIVED" // PIN получен, начинаем интервью
  | "INTERVIEWING"; // Проводим интервью

interface GenerateResponseParams {
  messageText: string;
  stage: ConversationStage;
  candidateName?: string;
  vacancyTitle?: string;
  responseStatus?: string;
  conversationHistory?: Array<{ sender: string; content: string }>;
  resumeData?: {
    experience?: string;
    coverLetter?: string;
    phone?: string;
  };
}

/**
 * Генерирует ответ через AI, имитируя живого рекрутера
 * Работает в 3 этапа:
 * 1. AWAITING_PIN - запрашивает PIN-код для идентификации
 * 2. PIN_RECEIVED - приветствует и начинает интервью
 * 3. INTERVIEWING - проводит интервью, задает вопросы
 */
export async function generateAIResponse(
  params: GenerateResponseParams,
): Promise<string> {
  const {
    messageText,
    stage,
    candidateName,
    vacancyTitle,
    responseStatus,
    conversationHistory = [],
    resumeData,
  } = params;

  const historyText =
    conversationHistory.length > 0
      ? conversationHistory
          .slice(-5)
          .map(
            (msg) =>
              `${msg.sender === "CANDIDATE" ? "Кандидат" : "Ты"}: ${msg.content}`,
          )
          .join("\n")
      : "";

  const hasVoiceMessages = conversationHistory.some((msg) =>
    msg.content.includes("[VOICE]"),
  );

  let stageInstructions = "";

  switch (stage) {
    case "AWAITING_PIN":
      stageInstructions = `
⚠️ ЭТАП 1: ОЖИДАНИЕ PIN-КОДА
Кандидат еще не идентифицирован. Ты не можешь начать интервью без идентификации.

ТВОЯ ЗАДАЧА:
- Поприветствуй дружелюбно как живой рекрутер
- Объясни, что для начала интервью нужен 4-значный код из письма
- Подчеркни важность кода для идентификации
- Будь терпеливым и помогающим
- НЕ используй технические термины (авторизация, система, база данных)

ПРИМЕРЫ ОТВЕТОВ:
"Привет! 👋 Чтобы начать интервью по вакансии, отправь, пожалуйста, 4-значный код из письма. Это нужно, чтобы я понял, с кем общаюсь."

"Привет! Вижу, ты интересуешься вакансией. Для начала отправь мне 4-значный код из письма, которое мы тебе отправили."`;
      break;

    case "PIN_RECEIVED":
      stageInstructions = `
⚠️ ЭТАП 2: PIN-КОД ПОЛУЧЕН — НАЧАЛО ИНТЕРВЬЮ
Кандидат только что отправил PIN-код. Система автоматически его проверила и идентифицировала кандидата.

ВНУТРЕННЯЯ ИНФОРМАЦИЯ (НЕ ПОКАЗЫВАЙ КАНДИДАТУ):
${candidateName ? `Имя: ${candidateName}` : "Имя неизвестно"}
${vacancyTitle ? `Вакансия: ${vacancyTitle}` : ""}
${resumeData?.experience ? `Опыт из резюме: ${resumeData.experience.slice(0, 200)}...` : ""}
${resumeData?.coverLetter ? `Сопроводительное письмо: ${resumeData.coverLetter.slice(0, 200)}...` : ""}

ТВОЯ ЗАДАЧА — ПРИТВОРИТЬСЯ ЖИВЫМ РЕКРУТЕРОМ:
- НЕ упоминай PIN-код, проверку, идентификацию или систему
- Веди себя так, будто ты просто продолжаешь обычное общение
- Тепло поприветствуй кандидата по имени
- Кратко упомяни вакансию (1 предложение)
- СРАЗУ попроси записать ГОЛОСОВОЕ сообщение для интервью
- Укажи 2-3 конкретных вопроса для голосового ответа
- Используй информацию из резюме для персонализации вопросов

ВОПРОСЫ ДЛЯ ГОЛОСОВОГО (выбери 2-3 релевантных):
- Расскажи подробнее о своем опыте работы
- Почему тебе интересна именно эта позиция?
- Какие у тебя ожидания по зарплате?
- Когда готов приступить к работе?
- Что тебя мотивирует в работе?

ПРИМЕР ОТВЕТА:
"Привет, ${candidateName || ""}! 👋 Рад, что откликнулся на ${vacancyTitle ? `"${vacancyTitle}"` : "нашу вакансию"}. Давай начнем знакомство — запиши голосовое сообщение и расскажи о своем опыте и почему тебе интересна эта позиция."`;
      break;

    case "INTERVIEWING":
      stageInstructions = `
⚠️ ЭТАП 3: ПРОВЕДЕНИЕ ИНТЕРВЬЮ
Кандидат идентифицирован. Проводи полноценное интервью.

КОНТЕКСТ:
${candidateName ? `Имя: ${candidateName}` : ""}
${vacancyTitle ? `Вакансия: ${vacancyTitle}` : ""}
${responseStatus ? `Статус: ${responseStatus}` : ""}
${resumeData?.experience ? `Опыт: ${resumeData.experience.slice(0, 300)}` : ""}
${hasVoiceMessages ? "✅ Кандидат уже отправлял голосовые сообщения" : "❌ Голосовых сообщений еще не было"}

ТВОЯ ЗАДАЧА КАК ЖИВОГО РЕКРУТЕРА:
${
  !hasVoiceMessages
    ? `
- ПРИОРИТЕТ: Попроси записать голосовое сообщение для интервью
- Объясни, что голосовое поможет лучше познакомиться
- Укажи конкретные вопросы для ответа`
    : `
- Отвечай на вопросы кандидата профессионально
- Задавай уточняющие вопросы по опыту и навыкам
- Используй информацию из резюме для персонализации
- Собирай информацию: опыт, навыки, зарплата, сроки выхода
- Предлагай следующие шаги (звонок, встреча, тестовое)`
}

СТИЛЬ ОБЩЕНИЯ:
- Пиши как ЖИВОЙ ЧЕЛОВЕК, не как бот
- Будь дружелюбным, но профессиональным
- Используй "ты" в обращении
- Отвечай кратко (1-3 предложения)
- Используй эмодзи умеренно (0-2 максимум)
- НЕ упоминай, что ты AI или бот
- НЕ используй шаблонные фразы
- Веди диалог естественно

${
  responseStatus === "COMPLETED"
    ? "⚠️ СТАТУС COMPLETED: Вежливо объясни, что процесс завершен"
    : ""
}
${
  responseStatus === "INTERVIEW_HH"
    ? "⚠️ СТАТУС INTERVIEW_HH: Предложи обсудить детали собеседования"
    : ""
}`;
      break;
  }

  const prompt = `Ты — ЖИВОЙ рекрутер компании, который общается с кандидатом в Telegram.

${historyText ? `ИСТОРИЯ ПЕРЕПИСКИ:\n${historyText}\n` : ""}

СООБЩЕНИЕ КАНДИДАТА:
${messageText}

${stageInstructions}

ФОРМАТ ОТВЕТА:
Верни только текст ответа без кавычек и пояснений.`;

  try {
    const { text } = await generateText({
      prompt,
      temperature: 0.7,
      generationName: "telegram-response",
      entityId: "telegram-chat",
      metadata: {
        messageText,
        candidateName,
        stage,
      },
    });

    return text.trim();
  } catch (error) {
    console.error("Ошибка генерации AI ответа:", error);
    return "Извини, что-то пошло не так. Можешь повторить?";
  }
}
