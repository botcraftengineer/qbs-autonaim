/**
 * Конфигурация группировки сообщений
 */

/** Минута в секундах */
const MINUTE = 60;

export const MESSAGE_GROUPING_CONFIG = {
  /**
   * Время ожидания после последнего ТЕКСТОВОГО сообщения (в секундах)
   */
  DEBOUNCE_DELAY_TEXT: 15,

  /**
   * Время ожидания после последнего ГОЛОСОВОГО сообщения (в секундах)
   * 
   * Дольше, потому что кандидат может записывать несколько голосовых подряд
   * (например, 2 голосовых по 5 минут с перерывом)
   */
  DEBOUNCE_DELAY_VOICE: 60,

  /**
   * Включить группировку сообщений
   * 
   * Группирует все сообщения кандидата с момента последнего ответа бота
   */
  ENABLE_GROUPING: true,
} as const;
