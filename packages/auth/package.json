{
  "name": "@qbs-autonaim/auth",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./middleware": "./src/middleware.ts",
    "./client": "./src/client.ts",
    "./env": "./env.ts"
  },
  "license": "MIT",
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "format": "biome format .",
    "lint": "biome lint .",
    "check": "biome check .",
    "generate": "bun run --env-file ../../.env bunx @better-auth/cli generate --config script/auth-cli.ts --output ../db/src/auth-schema.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@qbs-autonaim/config": "workspace:*",
    "@qbs-autonaim/db": "workspace:*",
    "better-auth": "1.4.7",
    "zod": "4.2.0"
  },
  "devDependencies": {
    "@better-auth/cli": "1.4.7",
    "@qbs-autonaim/tsconfig": "workspace:*",
    "typescript": "5.9.3"
  }
}
