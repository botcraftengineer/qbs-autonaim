/**
 * Промпты для форматирования резюме с HH.ru
 */

/**
 * Системный промпт для форматирования резюме
 */
export const RESUME_FORMATTER_SYSTEM_PROMPT = `Ты - эксперт по форматированию резюме. Твоя задача - преобразовать сырой HTML в чистый, структурированный HTML с правильными тегами.

КРИТИЧЕСКИ ВАЖНО:
- НЕ ВЫДУМЫВАЙ информацию
- НЕ ДОБАВЛЯЙ данные, которых нет в исходном тексте
- СОХРАНЯЙ весь текст из исходника
- Только очищай HTML и структурируй существующие данные
- Если информации нет - верни пустую строку

Отвечай ТОЛЬКО отформатированным HTML, без дополнительных комментариев.`;

/**
 * Промпт для форматирования опыта работы
 */
export function buildExperienceFormattingPrompt(rawHtml: string): string {
  return `Отформатируй опыт работы из HTML в структурированный текст с HTML тегами.

Используй следующую структуру:
<div class="experience">
  <div class="job">
    <h3>Должность</h3>
    <p class="company">Компания</p>
    <p class="period">Период работы</p>
    <ul class="responsibilities">
      <li>Обязанность 1</li>
      <li>Обязанность 2</li>
    </ul>
  </div>
</div>

Правила:
- Сохраняй ВСЕ данные из исходника
- НЕ добавляй информацию от себя
- Сохраняй хронологический порядок
- Группируй обязанности по смыслу
- Убери лишние пробелы и переносы
- Если информации нет, верни пустую строку

Исходный HTML:
${rawHtml}`;
}

/**
 * Промпт для форматирования языков
 */
export function buildLanguagesFormattingPrompt(rawHtml: string): string {
  return `Отформатируй информацию о языках в структурированный текст с HTML тегами.

Используй следующую структуру:
<div class="languages">
  <div class="language">
    <span class="name">Язык</span> — <span class="level">Уровень владения</span>
  </div>
</div>

Правила:
- Сохраняй ВСЕ языки из исходника
- НЕ добавляй языки от себя
- Один язык = одна строка
- Убери лишние элементы
- Если информации нет, верни пустую строку

Исходный HTML:
${rawHtml}`;
}

/**
 * Промпт для форматирования раздела "О себе"
 */
export function buildAboutFormattingPrompt(rawHtml: string): string {
  return `Отформатируй раздел "О себе" в структурированный текст с HTML тегами.

Используй следующую структуру:
<div class="about">
  <p>Текст о себе, разбитый на параграфы</p>
</div>

Правила:
- Сохраняй ВЕСЬ текст из исходника
- НЕ добавляй информацию от себя
- Разбивай длинный текст на параграфы по смыслу
- Убери лишние пробелы
- Сохраняй ключевые навыки и достижения
- Если информации нет, верни пустую строку

Исходный HTML:
${rawHtml}`;
}

/**
 * Промпт для форматирования образования
 */
export function buildEducationFormattingPrompt(rawHtml: string): string {
  return `Отформатируй образование в структурированный текст с HTML тегами.

Используй следующую структуру:
<div class="education">
  <div class="degree">
    <h3>Учебное заведение</h3>
    <p class="faculty">Факультет/Специальность</p>
    <p class="period">Годы обучения</p>
  </div>
</div>

Правила:
- Сохраняй ВСЕ данные об образовании из исходника
- НЕ добавляй информацию от себя
- Сохраняй хронологический порядок
- Группируй по учебным заведениям
- Если информации нет, верни пустую строку

Исходный HTML:
${rawHtml}`;
}

/**
 * Промпт для форматирования курсов
 */
export function buildCoursesFormattingPrompt(rawHtml: string): string {
  return `Отформатируй курсы и сертификаты в структурированный текст с HTML тегами.

Используй следующую структуру:
<div class="courses">
  <div class="course">
    <h4>Название курса</h4>
    <p class="organization">Организация</p>
    <p class="year">Год</p>
  </div>
</div>

Правила:
- Сохраняй ВСЕ курсы из исходника
- НЕ добавляй курсы от себя
- Один курс = один блок
- Сохраняй хронологический порядок
- Если информации нет, верни пустую строку

Исходный HTML:
${rawHtml}`;
}
