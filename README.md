# Selectio

Платформа для автоматизации работы с вакансиями на HH.ru. Позволяет автоматически входить в систему, парсить вакансии и отклики кандидатов.

## Что это?

Монорепозиторий на базе Bun и Turborepo для автоматизации рекрутинга:

- 🤖 Автоматический вход на HH.ru с сохранением сессии
- 📋 Парсинг активных вакансий и откликов
- 💼 Извлечение данных кандидатов (опыт, контакты, теги)
- 🎯 Автоматическая генерация промптов для скрининга резюме
- 🌐 Web-приложение на Next.js для управления

## Технологии

- **Bun** - быстрый JavaScript runtime
- **Next.js 16** - фронтенд приложение
- **Crawlee + Playwright** - автоматизация браузера
- **Drizzle ORM + Supabase** - база данных
- **Better Auth** - аутентификация

## Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd selectio

# Установить зависимости
bun install

# Настроить переменные окружения
cp .env.example .env
```

## Настройка

Отредактируйте файл `.env`:

```bash
# База данных (Supabase)
POSTGRES_URL="postgres://..."

# Секретный ключ (сгенерировать: openssl rand -base64 32)
AUTH_SECRET="your-secret-key"

# Учетные данные HH.ru
HH_EMAIL="your-email@example.com"
HH_PASSWORD="your-password"
```

## Запуск

```bash
# 1. Создать схему базы данных
bun db:push
bun auth:generate

# 2. Запустить приложение
bun dev
```

Приложение будет доступно по адресу: http://localhost:3000

## Автоматизация HH.ru

```bash
# Войти на HH.ru и сохранить cookies
bun hh:login

# Парсить вакансии и отклики
bun hh:example
```

## Скрининг резюме с AI

Автоматический скрининг резюме через DeepSeek AI:

```typescript
import { screenResumeWithAI } from "@selectio/jobs";

// Скрининг резюме
const result = await screenResumeWithAI("vacancy-id", {
  experience: "Senior Developer, 6 лет опыта",
  skills: "Node.js, TypeScript, PostgreSQL",
});

console.log(`Соответствие: ${result.match_percentage}%`);
console.log(`Рекомендация: ${result.recommendation}`);
```

📖 Быстрый старт: [QUICKSTART_AI.md](./QUICKSTART_AI.md)  
📖 Полная документация: [AI_INTEGRATION.md](./AI_INTEGRATION.md)

## Основные команды

```bash
bun dev              # Запустить в режиме разработки
bun build            # Собрать проект
bun db:push          # Обновить схему БД
bun db:studio        # Открыть Drizzle Studio
bun hh:login         # Войти на HH.ru
bun hh:example       # Использовать сохраненные cookies
```

## Структура проекта

```
selectio/
├── apps/nextjs/          # Next.js приложение
├── packages/
│   ├── jobs/             # Автоматизация HH.ru (Crawlee)
│   ├── api/              # tRPC API
│   ├── auth/             # Аутентификация
│   ├── db/               # База данных
│   └── ui/               # UI компоненты
└── tooling/              # Конфигурация (ESLint, Prettier, etc)
```

## Лицензия

MIT
